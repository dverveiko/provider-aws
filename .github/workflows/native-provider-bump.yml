name: Native Provider Version Bump

on:
  workflow_dispatch: {}

jobs:
  open-bump-pr:
    uses: upbound/uptest/.github/workflows/native-provider-bump.yml@8965579a213ba2b249225fa311178ac66f107daf # main
    with:
      provider-source: hashicorp/aws
      go-version: 1.19
    secrets:
      TOKEN: ${{ secrets.UPBOUND_BOT_GITHUB_TOKEN }}

